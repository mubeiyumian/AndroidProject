<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.green">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Green">
        <activity android:name=".FavoriteActivity"></activity>
        <activity android:name=".PublicDetailActivity" />
        <activity android:name=".PublicListActivity" />
        <activity android:name=".KnowledgeDetailActivity" />
        <activity android:name=".EcoKnowledgeActivity" />
        <activity android:name=".MyPublishActivity" />
        <activity android:name=".RecordsActivity" />
        <activity android:name=".PublishActivity" />
        <activity android:name=".ActivityDetail" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".AddEcoActivity" />
        <activity android:name=".EcoListActivity" />
        <activity android:name=".MyMedalsActivity" />
        <activity android:name=".PointsExchangeActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>